<!DOCTYPE html>
<html>
  <head>
    <!-- Outras meta tags, scripts, etc. -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  </head>
  <body>
    <div class="container">
      <h1><%= Coin.model_name.human(count: @coins.count) %></h1>
    
      <!-- Language links -->
      <div class="language-links">
        <%= link_to 'Português', root_path(locale: 'pt-BR') %>
        <%= link_to 'English', root_path(locale: 'en') %>
      </div>
    
      <!-- Table with custom styles -->
      <table class="table custom-table crypto-table">
        <thead>
          <tr>
            <th><%= @coins.human_attribute_name(:description) %></th>
            <th><%= @coins.human_attribute_name(:acronym) %></th>
            <th><%= @coins.human_attribute_name(:url_image) %></th>
          </tr>
        </thead>
        <tbody>
          <% @coins.each do |coin| %>
            <tr>
              <td><%= coin.description %></td>
              <td><%= coin.acronym %></td>
              <td><%= link_to(image_tag(coin.url_image, width: 25, height: 25), coin) %></td>
              <td><%= link_to t('links.show'), coin, class: 'no-underline d-grid gap-2 d-md-block' %></td>
              <td><%= link_to t('links.edit'), edit_coin_path(coin), class: 'no-underline d-grid gap-2 d-md-block' %></td>
              <td><%= link_to t('links.delete'), coin, method: :delete, data: { confirm: 'messages.confirm' }, class: 'no-underline d-grid gap-2 d-md-block'%></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    
      <!-- Button to create a new coin -->
      <%= link_to t('new.coin'), new_coin_path, class: "btn btn-primary" %>
    </div>
    
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
  </body>
</html>
